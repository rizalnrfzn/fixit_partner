import 'package:flutter/material.dart';

class ExpandedOrderListContainer extends StatelessWidget {
  const ExpandedOrderListContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
